# kwings-video-js

## Exporting

Frame-accurate MP4s, rendered offline rather than screen-recorded:

```bash
npm install
npm run studio          # then hit "Export MP4" in the page
```

or straight from the terminal:

```bash
npm run export:kwings
npm run export:invogen
```

See [tools/README.md](tools/README.md) for options.
